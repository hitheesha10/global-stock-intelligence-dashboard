streamlit
pandas
numpy
plotly
to install them we use:-
python.exe -m pip install --upgrade pip
pip install streamlit pandas plotly numpy
to run the code we use:-
python -m streamlit run app.py
streamlit run app.py
